<div class="container">
    <div class="row">
    <% if (users.length > 0) { %>
        <% users.map( users => { %>
        
                    <div class="col-sm">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title"><%= users.firstname %></h5>
                                <p class="card-text"><%= users.lastname %></p>
                                <a href="#" class="btn btn-primary"><%= users.age %></a>
                            </div>
                        </div>
                    </div>
   
        <% }) %>
       
    </div>
</div>          
    <% } else { %>
        <p class="card-text">Aucun utilisateur</p>
    <% } %>
